# GITHUB_TOKEN=
# PAT with `repo` scope for the private fork https://github.com/ABX-apps/UnrealEngine
# GH_TOKEN=   # accepted as an alias

# Optional overrides (defaults shown)
# UE_OWNER=ABX-apps
# UE_REPO=UnrealEngine
# UE_REF=release
# UE_FIXTURE=1

# Running Unreal Editor Remote Control HTTP (workstation or lab — not Grok Bot Linux)
# UE_REMOTE_CONTROL_URL=http://127.0.0.1:30010
